"use strict";
// Literals types
let myName;
let userName; // only specific values can be assign to username
userName = 'mahi7781';
class Msd {
    constructor(name, Number, Finisher) {
        this.name = name,
            this.Number = Number,
            this.Finisher = Finisher;
    }
}
// Actual create function
let msd = {
    name: "Dhoni",
    Number: 7,
    Finisher: true
};
let smith = {
    name: "Smith",
    Finisher: false
};
